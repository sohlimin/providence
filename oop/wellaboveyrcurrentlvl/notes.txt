Epsilon is normally used to represent very small values greater than zero.
Epsilon is represented in lowercase as ϵ, and in uppercase as E.

for (File item : dir.listFiles()) { 

}

equivalent to:

File[] files = dir.listFiles()

for (int i = 0 ; i < files.length ; i ++) {
	File item = files[i];
}

________________________________________________Array_________________________________________________

Class_name[] variable = {"blah", "blah"}

//Elements must be of the same data type of Class_name
//Different way: declare the number of elements for your array, then assign values later.

Class_name[] variable = new Class_name[1];

variable[0] = "blah";
variable[1] = "blah';

for (int i = 0 ; i < variable.length ; i++) {
	System.out.println(variable[i]);
}

_______________________________________________ArrayList________________________________________________
//Arrays - fixed size. ArrayList - resizeable.
//import java.util.ArrayList;

ArrayList<String> variable = new ArrayList<>();

The type of the ArrayList variable is ArrayList. When a list variable is initialised, the type of the values to be stored is defined  - all the variable stored in a given list are of the same type. As such, the type of an ArrayList that stores strings is "ArrayList<String>". A new list is created with the command "new ArrayList<>();" 


variable.add("blah");

variable.remove(0); //removing an element at index 0

variable.set(0,"meh"); //replaces element at index 0 with "meh"

System.out.println(variable.get(0)); //gets an element at a certain index 

System.out.println(fruits.size()); //gets the number of elements in the ArrayList

//To sort your ArrayList, use the collections framework called the sort method
// import java.util.Collections

Collections.sort(fruits); // sorts in alphabetical order

//To iterate through all the elements of an ArrayList, use an enhanced for loop.

for (String abc : variable) { //for (Class_name or data type of the ArrayList. for every abc (local variable) in my Arra				//yList variable), do action in curly braces. here print out fruit 

}

